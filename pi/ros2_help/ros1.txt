sudo apt update
sudo apt install -y docker.io

sudo systemctl start docker
sudo systemctl enable docker

sudo docker pull ros:noetic
sudo docker run -it --rm ros:noetic

rostopic list
rosnode list
rospack list

roscore
bg
fg

sudo apt update
sudo apt install git
rosdep update

mkdir -p ws/src
cd ws/src
git clone https://github.com/tu-darmstadt-ros-pkg/hector_slam.git
cd ..
rosdep install --from-paths src --ignore-src -r -y

export DISPLAY=:0
xhost +local:docker

sudo docker run -it --rm --device=/dev/ttyUSB0 -e DISPLAY=$DISPLAY -v /tmp/.X11-unix:/tmp/.X11-unix ros:noetic
sudo apt update
sudo apt install ros-noetic-rplidar-ros -y
sudo apt install ros-noetic-rviz -y
rviz
roslaunch rplidar_ros view_rplidar_a1.launch

