sudo apt update -y
sudo apt install ros-jazzy-navigation2 -y
sudo apt install ros-jazzy-nav2-bringup -y

# Then in another terminal, launch Nav2
ros2 launch nav2_bringup navigation_launch.py use_sim_time:=false autostart:=true

sudo nano /opt/ros/jazzy/share/nav2_bringup/params/nav2_params.yaml
collision_monitor:
  ros__parameters:
    transform_tolerance: 0.2 -> 0.6

ros2 topic echo /cmd_vel